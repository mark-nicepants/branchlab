{
  "name": "branchlab-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && python3 -m http.server 8899 -d dist",
    "preview": "python3 -m http.server 8899 -d dist",
    "shots": "node screenshots.mjs"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "playwright": "^1.62.1",
    "sharp": "^0.35.3"
  }
}
